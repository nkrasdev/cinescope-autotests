# Contributing to Cinescope Autotests

Спасибо за интерес к проекту! Мы приветствуем вклад в развитие тестов.

## Требования к окружению

- Python 3.13+
- uv (package manager)
- Node.js (для Allure Report)
- Git

## Начало работы

### 1. Клонирование репозитория

```bash
git clone https://github.com/nkrasdev/cinescope-autotests.git
cd cinescope-autotests
```

### 2. Установка зависимостей

```bash
make install
# или
uv sync --dev
```

### 3. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env с вашими креденшилами
```

### 4. Установка pre-commit hooks

```bash
make pre-commit-install
# или
uv run pre-commit install
```

## Workflow разработки

### 1. Создание ветки

```bash
git checkout -b feature/my-new-feature
# или
git checkout -b fix/bug-description
```

### 2. Разработка

Напишите свой код, следуя:
- PEP 8 (проверяется Ruff)
- Type hints (проверяется mypy)
- Тесты должны быть понятными и иметь Allure декораторы

### 3. Проверка кода перед коммитом

```bash
# Форматирование
make format

# Все проверки
make check-all

# Запуск тестов
make test-cov
```

### 4. Коммит изменений

```bash
git add .
git commit -m "feat: add new test for movie creation"
```

Pre-commit hooks автоматически запустят проверки.

### 5. Пуш и создание PR

```bash
git push origin feature/my-new-feature
```

Создайте Pull Request на GitHub.

## Стиль коммитов

Используем Conventional Commits:

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `test:` - добавление/изменение тестов
- `refactor:` - рефакторинг кода
- `docs:` - изменения в документации
- `chore:` - обновление зависимостей, конфигурации

Примеры:
```
feat: add test for user authentication
fix: correct movie deletion test assertion
test: add parametrized test for invalid movie data
docs: update README with new commands
```

## Структура тестов

### API тесты

```python
import allure
from tests.utils.decorators import allure_test_details

@allure.epic("Фильмы")
@allure.feature("Создание фильма")
class TestCreateMovie:
    @allure_test_details(
        story="Успешное создание",
        title="Тест создания фильма с валидными данными",
        description="Подробное описание теста",
        severity=allure.severity_level.CRITICAL,
    )
    def test_create_movie_success(self, admin_api_manager, movie_payload):
        # Ваш тест
        pass
```

### UI тесты

```python
import pytest
import allure
from tests.ui.pages.movies_page import MoviesPage

@pytest.mark.ui
@allure.epic("UI")
@allure.feature("Фильмы")
class TestMoviesPage:
    def test_movie_display(self, page):
        movies_page = MoviesPage(page)
        movies_page.open()
        # Ваш тест
        pass
```

## Checklist перед PR

- [ ] Код отформатирован (`make format`)
- [ ] Линтер не выдает ошибок (`make lint`)
- [ ] Type checking проходит (`make type-check`)
- [ ] Security check проходит (`make security`)
- [ ] Все тесты проходят (`make test-api`)
- [ ] Добавлены Allure декораторы
- [ ] Обновлена документация (если нужно)
- [ ] Pre-commit hooks установлены и проходят

## Полезные команды

```bash
make help                # Показать все доступные команды
make install             # Установить зависимости
make lint                # Запустить линтер
make format              # Отформатировать код
make type-check          # Проверить типы
make security            # Проверить безопасность
make test-api            # Запустить API тесты
make test-ui             # Запустить UI тесты
make test-cov            # Запустить тесты с coverage
make test-parallel       # Запустить тесты параллельно
make check-all           # Запустить все проверки
make clean               # Очистить временные файлы
make ci-local            # Запустить все проверки как в CI
```

## Code Review

При ревью кода обращаем внимание на:

1. **Читаемость**: Код должен быть понятным
2. **Типизация**: Все функции должны иметь type hints
3. **Документация**: Сложная логика должна быть задокументирована
4. **Тесты**: Новая функциональность должна быть покрыта тестами
5. **DRY**: Избегайте дублирования кода
6. **Безопасность**: Не храните секреты в коде

## Вопросы?

Если у вас есть вопросы, создайте Issue на GitHub.

Спасибо за ваш вклад! 🚀
